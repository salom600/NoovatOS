# Calamares — welcome module
showSupportUrl: true
showKnownIssuesUrl: true
showReleaseNotesUrl: true

requirements:
  requiredStorage: 8
  requiredRam: 1.5
  internetCheck: false   # Don't require internet for install
  rootCheck: true

requirementsLabel:
  entries:
    - storage: "Requires at least 8 GB of storage"
    - ram: "Requires at least 1.5 GB of RAM"
    - root: "Must be run as root"
