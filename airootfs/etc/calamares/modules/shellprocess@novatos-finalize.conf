# Calamares — shellprocess module: final commands before finish
dontChroot: false

script:
  - "-mkdir -p /mnt/var/lib/novatos && touch /mnt/var/lib/novatos/installed.flag"
  - "-chroot /mnt systemctl enable sddm"
  - "-chroot /mnt plymouth-set-default-theme -R spinner"
