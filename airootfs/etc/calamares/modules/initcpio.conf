# Calamares — initcpio module (rebuild initramfs on installed system)
# We use the same mkinitcpio.conf hooks as the live ISO.

kernel: linux

initramfsName: /boot/initramfs-linux.img
fallbackInitramfsName: /boot/initramfs-linux-fallback.img
